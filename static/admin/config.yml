backend:
  name: github # Refer to https://www.netlifycms.org/docs/authentication-backends/ for auth backend list and instructions
  branch: master # Branch to update
  repo: rawsta/rawblog # Repo for pushing new commits. Make sure to replace with your repo!

media_folder: static/assets
public_folder: assets

collections:
  - name: blog
    label: "Post"
    folder: "content"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}.md"
    fields:
      - { label: "Titel", name: "title", widget: "string" }
      - { label: "Publish Datum", name: "date", widget: "datetime" }
      - { label: "Cover Bild", name: "cover", widget: "image" }
      - { label: "Slug", name: "slug", widget: "string" }
      - { label: "Category", name: "category", widget: "string" }
      - { label: "HashTags", name: "tags", widget: "list" }
      - { label: "Body", name: "body", widget: "markdown" }
